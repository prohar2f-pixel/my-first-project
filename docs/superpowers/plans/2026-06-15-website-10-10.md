# Website 10/10 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix 7 concrete issues found in the code-review so the site scores 10/10 across security, SEO, accessibility and code quality.

**Architecture:** Static GitHub Pages site (`index.html` + `assets/`). The only back-end concern is the Telegram form submission, which will be proxied through a free Cloudflare Worker so the bot token never reaches the browser.

**Tech Stack:** Plain HTML/CSS/JS, Cloudflare Workers (free tier), `wrangler` CLI for deploy.

---

## Files map

| Action | Path | Responsibility |
|--------|------|---------------|
| Modify | `index.html` | SEO tags, h1, favicon link, dead link, form labels, form fetch URL |
| Modify | `assets/fluid.js:30` | `getElementsByTagName` → `getElementById` |
| Create | `assets/LDR_LLL1_0.png` | Dithering texture (download from original repo) |
| Create | `assets/favicon.svg` | SVG favicon АП |
| Create | `cloudflare-worker/worker.js` | Telegram proxy (stores token server-side) |
| Create | `cloudflare-worker/wrangler.toml` | Cloudflare Worker config |
| Delete | `assets/smoke_cta.jpg` | Unused file (never referenced in HTML) |

---

### Task 1: Download missing dithering texture

**Files:**
- Create: `assets/LDR_LLL1_0.png`

`fluid.js:863` calls `createTextureAsync('LDR_LLL1_0.png')` but the file is missing from `assets/`. This causes a silent WebGL error that breaks the smoke animation.

- [ ] **Step 1: Download the texture**

```bash
curl -L "https://raw.githubusercontent.com/PavelDoGreat/WebGL-Fluid-Simulation/master/LDR_LLL1_0.png" \
  -o "assets/LDR_LLL1_0.png"
```

Expected: file `assets/LDR_LLL1_0.png` (64×64 blue-noise PNG, ~3 KB) appears in folder.

- [ ] **Step 2: Verify in browser**

Open `index.html` locally, open DevTools → Console. Confirm no `Failed to load resource: LDR_LLL1_0.png` error.

- [ ] **Step 3: Commit**

```bash
git add assets/LDR_LLL1_0.png
git commit -m "fix: add missing LDR_LLL1_0.png dithering texture for WebGL fluid"
```

---

### Task 2: Fix fluid.js — getElementById

**Files:**
- Modify: `assets/fluid.js:30`

`getElementsByTagName('canvas')[0]` is fragile — if another canvas is added it breaks. The canvas already has `id="smokeCanvas"` in `index.html:1314`.

- [ ] **Step 1: Edit `assets/fluid.js` line 30**

Old:
```js
const canvas = document.getElementsByTagName('canvas')[0];
```

New:
```js
const canvas = document.getElementById('smokeCanvas');
```

- [ ] **Step 2: Verify in browser**

Open `index.html`. The smoke WebGL animation should still render on page load (move cursor across screen to trigger fluid).

- [ ] **Step 3: Commit**

```bash
git add assets/fluid.js
git commit -m "fix: use getElementById instead of getElementsByTagName for smokeCanvas"
```

---

### Task 3: Create SVG favicon

**Files:**
- Create: `assets/favicon.svg`
- Modify: `index.html` (add `<link rel="icon">` in `<head>`)

- [ ] **Step 1: Create `assets/favicon.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="6" fill="#0a0a0a"/>
  <text x="16" y="23" font-family="Arial Black,Arial,sans-serif" font-weight="900" font-size="14" fill="#a855f7" text-anchor="middle">АП</text>
</svg>
```

- [ ] **Step 2: Add favicon link in `index.html`**

Insert after line 6 (`<meta name="viewport" ...>`):

```html
<link rel="icon" href="assets/favicon.svg" type="image/svg+xml">
```

- [ ] **Step 3: Verify**

Open `index.html` in browser. The browser tab should show a small purple "АП" icon.

- [ ] **Step 4: Commit**

```bash
git add assets/favicon.svg index.html
git commit -m "feat: add SVG favicon"
```

---

### Task 4: Add SEO meta tags + canonical + Open Graph

**Files:**
- Modify: `index.html` (add 8 lines to `<head>`)

- [ ] **Step 1: Add all meta tags to `index.html`**

Insert after `<link rel="icon" ...>` (after favicon line added in Task 3):

```html
<meta name="description" content="Александр Прохоров — создаю современные сайты под ключ и внедряю нейросети для бизнеса. Разработка сайтов от 30 000 ₽, быстрый запуск и результат.">
<link rel="canonical" href="https://prohar2f.github.io/my-first-project/">
<meta property="og:type" content="website">
<meta property="og:url" content="https://prohar2f.github.io/my-first-project/">
<meta property="og:title" content="Александр Прохоров — Сайты и нейросети для бизнеса">
<meta property="og:description" content="Создаю современные сайты под ключ и внедряю нейросети для бизнеса. Разработка от 30 000 ₽.">
<meta property="og:image" content="https://prohar2f.github.io/my-first-project/assets/photo.jpg">
```

- [ ] **Step 2: Verify with browser DevTools**

In DevTools → Elements, confirm `<meta name="description">` and `<meta property="og:title">` appear in `<head>`.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: add meta description, canonical URL and Open Graph tags"
```

---

### Task 5: Fix semantic heading (h1) and dead link

**Files:**
- Modify: `index.html`

Two issues in one commit:
1. The hero name uses `<div>` instead of `<h1>` — hurts SEO crawlers.
2. `<a href="#">будут обновляться</a>` on line 969 is a dead link (points to `#`).

- [ ] **Step 1: Fix hero heading in `index.html`**

Find (lines 547–548):
```html
    <div class="hn1">Александр</div>
    <div class="hn2">Прохоров</div>
```

Replace with:
```html
    <h1 style="margin:0;padding:0"><span class="hn1" style="display:block">Александр</span><span class="hn2" style="display:block">Прохоров</span></h1>
```

- [ ] **Step 2: Fix dead link in pricing section**

Find (line 969):
```html
По мере роста портфолио цены <a href="#">будут обновляться</a>.
```

Replace with:
```html
По мере роста портфолио цены будут обновляться.
```

(Remove the `<a>` — it's a non-functional decoration.)

- [ ] **Step 3: Verify in browser**

Open page. Hero name should still look identical (h1 with display:block spans). Pricing section text should have no underline on "будут обновляться".

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "fix: use h1 for hero name, remove dead href=# link in pricing"
```

---

### Task 6: Fix form label accessibility

**Files:**
- Modify: `index.html` (order modal form, lines 1212–1237)

All 5 form labels are missing `for` attributes, so clicking a label doesn't focus the input (accessibility failure).

- [ ] **Step 1: Add `for` attributes to all labels in the modal form**

Find and replace each label:

```html
<!-- Before: -->
<label>Ваше имя *</label>
<input type="text" id="fname" ...>

<!-- After: -->
<label for="fname">Ваше имя *</label>
<input type="text" id="fname" ...>
```

Full diff — replace all 5 labels (lines 1212, 1216, 1220, 1224, 1234):

| Old | New |
|-----|-----|
| `<label>Ваше имя *</label>` | `<label for="fname">Ваше имя *</label>` |
| `<label>Телефон / Telegram *</label>` | `<label for="fphone">Телефон / Telegram *</label>` |
| `<label>Эл. почта</label>` | `<label for="femail">Эл. почта</label>` |
| `<label>Услуга</label>` | `<label for="fservice">Услуга</label>` |
| `<label>Комментарий</label>` | `<label for="fcomment">Комментарий</label>` |

- [ ] **Step 2: Verify**

Open modal (click "Заказать"). Click the text "Ваше имя *" — the name input should receive focus.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "fix: add for attributes to all form labels for accessibility"
```

---

### Task 7: Remove exposed bot token — Cloudflare Worker proxy

**Files:**
- Create: `cloudflare-worker/worker.js`
- Create: `cloudflare-worker/wrangler.toml`
- Modify: `index.html` (replace `const BOT / CID` + fetch call)

This is the only critical security issue. The bot token `8891745478:AAHpXUI1ZZ6rLnlh6ex3Dti...` is currently hardcoded in client-side JS and visible to anyone who views source.

**Plan:** Create a Cloudflare Worker that holds the token as an env secret and proxies the message. The frontend sends structured JSON to the Worker URL; the Worker forwards it to Telegram.

#### Sub-task 7a: Create the Worker code

- [ ] **Step 1: Create `cloudflare-worker/worker.js`**

```js
export default {
  async fetch(request, env) {
    if (request.method === 'OPTIONS') {
      return new Response(null, {
        headers: {
          'Access-Control-Allow-Origin': '*',
          'Access-Control-Allow-Methods': 'POST, OPTIONS',
          'Access-Control-Allow-Headers': 'Content-Type',
        },
      });
    }

    if (request.method !== 'POST') {
      return new Response('Method Not Allowed', { status: 405 });
    }

    const cors = {
      'Access-Control-Allow-Origin': '*',
      'Content-Type': 'application/json',
    };

    let data;
    try {
      data = await request.json();
    } catch {
      return new Response(JSON.stringify({ ok: false, error: 'bad json' }), { status: 400, headers: cors });
    }

    const { name, phone, email, service, comment } = data;
    const text = [
      '🛒 <b>Новая заявка с сайта!</b>',
      `👤 <b>Имя:</b> ${name}`,
      `📞 <b>Контакт:</b> ${phone}`,
      email ? `📧 <b>Email:</b> ${email}` : null,
      `💼 <b>Услуга:</b> ${service}`,
      comment ? `💬 <b>Комментарий:</b> ${comment}` : null,
    ].filter(Boolean).join('\n\n');

    try {
      await fetch(`https://api.telegram.org/bot${env.TELEGRAM_BOT_TOKEN}/sendMessage`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ chat_id: env.TELEGRAM_CHAT_ID, text, parse_mode: 'HTML' }),
      });
      return new Response(JSON.stringify({ ok: true }), { headers: cors });
    } catch (err) {
      return new Response(JSON.stringify({ ok: false }), { status: 500, headers: cors });
    }
  },
};
```

- [ ] **Step 2: Create `cloudflare-worker/wrangler.toml`**

```toml
name = "prohar-contact-form"
main = "worker.js"
compatibility_date = "2024-01-01"
```

#### Sub-task 7b: Deploy the Worker

- [ ] **Step 3: Install wrangler and log in**

```bash
npm install -g wrangler
wrangler login
```

Expected: browser opens Cloudflare login page. Authenticate with your Cloudflare account (free, no credit card).

- [ ] **Step 4: Deploy the Worker**

```bash
cd cloudflare-worker
wrangler deploy
```

Expected output includes:
```
Published prohar-contact-form (...)
  https://prohar-contact-form.<YOUR_SUBDOMAIN>.workers.dev
```

Copy the Worker URL — you'll need it in Step 6.

- [ ] **Step 5: Set the bot token and chat ID as secrets (never stored in code)**

```bash
wrangler secret put TELEGRAM_BOT_TOKEN
# Paste: YOUR_BOT_TOKEN_HERE
# Press Enter

wrangler secret put TELEGRAM_CHAT_ID
# Paste: YOUR_CHAT_ID_HERE
# Press Enter
```

Expected: `✓ Success! Uploaded secret TELEGRAM_BOT_TOKEN`

#### Sub-task 7c: Update index.html

- [ ] **Step 6: Replace bot credentials and fetch call in `index.html`**

Find (lines 1270–1271):
```js
  const BOT = 'YOUR_BOT_TOKEN_HERE';
  const CID = 'YOUR_CHAT_ID_HERE';
```

Replace with (substitute `<YOUR_WORKER_URL>` with the URL from Step 4):
```js
  const WORKER_URL = 'https://prohar-contact-form.<YOUR_SUBDOMAIN>.workers.dev';
```

Find (lines 1302–1305):
```js
      await fetch(`https://api.telegram.org/bot${BOT}/sendMessage`,{
        method:'POST',headers:{'Content-Type':'application/json'},
        body:JSON.stringify({chat_id:CID,text,parse_mode:'HTML'})
      });
```

Replace with:
```js
      await fetch(WORKER_URL,{
        method:'POST',headers:{'Content-Type':'application/json'},
        body:JSON.stringify({name,phone,email,service:fservice.value,comment})
      });
```

Also remove the now-unused `text` variable construction above (lines 1300–1301) to keep the code clean:

```js
// DELETE these two lines:
    const text=`🛒 <b>Новая заявка с сайта!</b>\n\n...`;
```

- [ ] **Step 7: Test the form end-to-end**

Open `index.html` (deployed on GitHub Pages or locally via a local server — note: won't work on `file://` due to CORS). Click "Заказать", fill in the form, submit. Confirm a Telegram message arrives in your chat.

- [ ] **Step 8: Commit**

```bash
cd ..
git add cloudflare-worker/ index.html
git commit -m "security: move Telegram bot token to Cloudflare Worker, remove from client-side JS"
```

---

### Task 8: Delete unused asset

**Files:**
- Delete: `assets/smoke_cta.jpg`

`smoke_cta.jpg` (exists in `assets/`) is never referenced in `index.html` or `fluid.js`. Dead weight on the repo and GitHub Pages CDN.

- [ ] **Step 1: Delete the file**

```bash
git rm assets/smoke_cta.jpg
```

- [ ] **Step 2: Verify**

```bash
grep -r "smoke_cta" . --include="*.html" --include="*.js"
```

Expected: no output (file not referenced anywhere).

- [ ] **Step 3: Commit**

```bash
git commit -m "chore: remove unused smoke_cta.jpg"
```

---

### Task 9: Push and verify on GitHub Pages

- [ ] **Step 1: Push all commits**

```bash
git push origin main
```

- [ ] **Step 2: Check GitHub Actions / Pages build**

Go to `https://github.com/prohar2f/my-first-project/actions` and wait for the Pages deploy to finish (~1 min).

- [ ] **Step 3: Verify live site**

Open `https://prohar2f.github.io/my-first-project/` and confirm:
- Browser tab shows "АП" favicon
- DevTools → Elements shows `<meta name="description">` in `<head>`
- DevTools → Console shows no `LDR_LLL1_0.png` 404 error
- Smoke animation plays on cursor movement
- Form submits → Telegram message arrives
- Page source has no bot token string

---

## Self-review

**Spec coverage:**
| Issue | Task |
|-------|------|
| Bot token in client-side JS | Task 7 |
| Missing `LDR_LLL1_0.png` | Task 1 |
| No `<meta description>` | Task 4 |
| No Open Graph tags | Task 4 |
| No `<link rel="canonical">` | Task 4 |
| No favicon | Task 3 |
| Hero name uses `<div>` not `<h1>` | Task 5 |
| Dead `href="#"` link | Task 5 |
| Form labels missing `for` | Task 6 |
| `getElementsByTagName` in fluid.js | Task 2 |
| Unused `smoke_cta.jpg` | Task 8 |

**Placeholder scan:** All steps contain concrete commands or code. No "TBD" entries.

**Type consistency:** `WORKER_URL` defined once (Task 7, Step 6) and used once in the fetch call same step. No naming drift.
