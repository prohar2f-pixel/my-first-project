# AI Image Generator Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an AI image generation page to the site using Runware klingai-image-3-0, with login, credits system, history/gallery, and admin panel.

**Architecture:** Static HTML frontend (3 pages in `image-gen/`) calls a FastAPI backend (`image-gen-api/`) deployed on Railway. Backend calls Runware API for image generation and stores results in PostgreSQL. Auth via JWT tokens stored in localStorage.

**Tech Stack:** Python 3.11, FastAPI, SQLAlchemy 2.0, PostgreSQL, python-jose (JWT), passlib (bcrypt), httpx, pytest. Frontend: vanilla HTML/CSS/JS, no framework.

---

## File Structure

```
my-first-project/
  image-gen/
    login.html          — login page
    index.html          — generator (prompt, settings, result, history, gallery)
    admin.html          — admin panel (create users, manage credits)

  image-gen-api/
    main.py             — FastAPI app, all routes, startup seeding
    models.py           — SQLAlchemy models: User, Generation
    auth.py             — password hashing, JWT create/decode
    database.py         — engine, SessionLocal, get_db dependency
    runware.py          — Runware API client
    requirements.txt
    env.example
    nixpacks.toml       — Railway build config
    Procfile
    tests/
      conftest.py       — test client, in-memory SQLite, seed fixtures
      test_auth.py      — login endpoint tests
      test_generate.py  — generate endpoint tests (Runware mocked)
      test_history.py   — history + gallery tests
      test_admin.py     — admin endpoint tests
```

---

## Task 1: Project skeleton + database layer

**Files:**
- Create: `image-gen-api/requirements.txt`
- Create: `image-gen-api/env.example`
- Create: `image-gen-api/nixpacks.toml`
- Create: `image-gen-api/Procfile`
- Create: `image-gen-api/database.py`
- Create: `image-gen-api/models.py`

- [ ] **Step 1: Create `image-gen-api/` folder and `requirements.txt`**

```
fastapi==0.111.0
uvicorn==0.29.0
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.6
python-dotenv==1.0.1
```

- [ ] **Step 2: Create `image-gen-api/env.example`**

```
DATABASE_URL=postgresql://user:password@host:5432/dbname
RUNWARE_API_KEY=your-runware-api-key
JWT_SECRET=change-me-to-a-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
SEED_ADMIN=true
```

- [ ] **Step 3: Create `image-gen-api/nixpacks.toml`**

```toml
[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
```

- [ ] **Step 4: Create `image-gen-api/Procfile`**

```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

- [ ] **Step 5: Create `image-gen-api/database.py`**

```python
import os
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from dotenv import load_dotenv

load_dotenv()

DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./dev.db")

# Railway gives postgres://, SQLAlchemy needs postgresql://
if DATABASE_URL.startswith("postgres://"):
    DATABASE_URL = DATABASE_URL.replace("postgres://", "postgresql://", 1)

connect_args = {"check_same_thread": False} if DATABASE_URL.startswith("sqlite") else {}
engine = create_engine(DATABASE_URL, connect_args=connect_args)
SessionLocal = sessionmaker(bind=engine)

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

- [ ] **Step 6: Create `image-gen-api/models.py`**

```python
from sqlalchemy import Column, Integer, String, Boolean, DateTime, JSON
from sqlalchemy.orm import DeclarativeBase
from datetime import datetime

class Base(DeclarativeBase):
    pass

class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True, index=True)
    username = Column(String, unique=True, nullable=False)
    password_hash = Column(String, nullable=False)
    credits = Column(Integer, default=0)
    is_admin = Column(Boolean, default=False)
    created_at = Column(DateTime, default=datetime.utcnow)

class Generation(Base):
    __tablename__ = "generations"
    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, nullable=False)
    prompt = Column(String, nullable=False)
    negative_prompt = Column(String, default="")
    settings = Column(JSON, default={})
    image_urls = Column(JSON, default=[])
    created_at = Column(DateTime, default=datetime.utcnow)
```

- [ ] **Step 7: Install deps and verify SQLite dev DB creates ok**

```bash
cd image-gen-api
pip install -r requirements.txt
python -c "from database import engine; from models import Base; Base.metadata.create_all(engine); print('OK')"
```

Expected: `OK`

- [ ] **Step 8: Commit**

```bash
git add image-gen-api/
git commit -m "feat(image-gen): backend skeleton — db models, requirements"
```

---

## Task 2: Auth helpers

**Files:**
- Create: `image-gen-api/auth.py`
- Create: `image-gen-api/tests/conftest.py`
- Create: `image-gen-api/tests/test_auth_helpers.py`

- [ ] **Step 1: Write failing test for auth helpers**

Create `image-gen-api/tests/test_auth_helpers.py`:

```python
from auth import hash_password, verify_password, create_token, decode_token

def test_password_round_trip():
    hashed = hash_password("secret")
    assert verify_password("secret", hashed) is True
    assert verify_password("wrong", hashed) is False

def test_token_round_trip():
    token = create_token(user_id=42, is_admin=True)
    payload = decode_token(token)
    assert payload["sub"] == "42"
    assert payload["is_admin"] is True

def test_invalid_token_raises():
    import pytest
    with pytest.raises(Exception):
        decode_token("not.a.token")
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd image-gen-api
pytest tests/test_auth_helpers.py -v
```

Expected: `ImportError: cannot import name 'hash_password' from 'auth'`

- [ ] **Step 3: Create `image-gen-api/auth.py`**

```python
import os
from jose import jwt, JWTError
from passlib.context import CryptContext
from datetime import datetime, timedelta

SECRET_KEY = os.getenv("JWT_SECRET", "dev-secret")
ALGORITHM = "HS256"
TOKEN_EXPIRE_HOURS = 24 * 7

pwd_context = CryptContext(schemes=["bcrypt"])

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(password: str, hashed: str) -> bool:
    return pwd_context.verify(password, hashed)

def create_token(user_id: int, is_admin: bool) -> str:
    payload = {
        "sub": str(user_id),
        "is_admin": is_admin,
        "exp": datetime.utcnow() + timedelta(hours=TOKEN_EXPIRE_HOURS),
    }
    return jwt.encode(payload, SECRET_KEY, algorithm=ALGORITHM)

def decode_token(token: str) -> dict:
    return jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
```

- [ ] **Step 4: Run test to verify it passes**

```bash
pytest tests/test_auth_helpers.py -v
```

Expected: 3 PASSED

- [ ] **Step 5: Create `image-gen-api/tests/conftest.py`**

```python
import os
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

os.environ.setdefault("JWT_SECRET", "test-secret")
os.environ.setdefault("SEED_ADMIN", "false")

from main import app
from database import get_db
from models import Base, User, Generation
from auth import hash_password

TEST_DB_URL = "sqlite:///./test.db"
test_engine = create_engine(TEST_DB_URL, connect_args={"check_same_thread": False})
TestingSession = sessionmaker(bind=test_engine)

def override_get_db():
    db = TestingSession()
    try:
        yield db
    finally:
        db.close()

@pytest.fixture
def client():
    Base.metadata.create_all(bind=test_engine)
    app.dependency_overrides[get_db] = override_get_db

    db = TestingSession()
    db.add(User(username="admin", password_hash=hash_password("admin123"), is_admin=True, credits=0))
    db.add(User(username="client1", password_hash=hash_password("pass123"), is_admin=False, credits=10))
    db.commit()
    db.close()

    yield TestClient(app)

    app.dependency_overrides.clear()
    Base.metadata.drop_all(bind=test_engine)
```

- [ ] **Step 6: Commit**

```bash
git add image-gen-api/auth.py image-gen-api/tests/
git commit -m "feat(image-gen): auth helpers + test conftest"
```

---

## Task 3: Main app + login endpoint

**Files:**
- Create: `image-gen-api/main.py`
- Create: `image-gen-api/tests/test_auth.py`

- [ ] **Step 1: Write failing login tests**

Create `image-gen-api/tests/test_auth.py`:

```python
def test_login_success(client):
    resp = client.post("/api/login", json={"username": "admin", "password": "admin123"})
    assert resp.status_code == 200
    data = resp.json()
    assert "token" in data
    assert data["username"] == "admin"
    assert data["is_admin"] is True
    assert "credits" in data

def test_login_wrong_password(client):
    resp = client.post("/api/login", json={"username": "admin", "password": "wrong"})
    assert resp.status_code == 401

def test_login_unknown_user(client):
    resp = client.post("/api/login", json={"username": "nobody", "password": "x"})
    assert resp.status_code == 401

def test_protected_route_no_token(client):
    resp = client.get("/api/history")
    assert resp.status_code in (401, 422)
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/test_auth.py -v
```

Expected: `ImportError` or `404` — main.py doesn't exist yet

- [ ] **Step 3: Create `image-gen-api/main.py`**

```python
import os
from contextlib import asynccontextmanager
from fastapi import FastAPI, Depends, HTTPException, Header
from fastapi.middleware.cors import CORSMiddleware
from sqlalchemy.orm import Session
from pydantic import BaseModel

from database import get_db, engine
from models import Base, User, Generation
from auth import hash_password, verify_password, create_token, decode_token

CREDITS_PER_IMAGE = 1

@asynccontextmanager
async def lifespan(app: FastAPI):
    Base.metadata.create_all(bind=engine)
    if os.getenv("SEED_ADMIN", "true") == "true":
        db = next(get_db())
        username = os.getenv("ADMIN_USERNAME", "admin")
        if not db.query(User).filter(User.username == username).first():
            db.add(User(
                username=username,
                password_hash=hash_password(os.getenv("ADMIN_PASSWORD", "change-me")),
                is_admin=True,
                credits=0,
            ))
            db.commit()
        db.close()
    yield

app = FastAPI(lifespan=lifespan)
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

def get_current_user(authorization: str = Header(...), db: Session = Depends(get_db)) -> User:
    try:
        token = authorization.replace("Bearer ", "")
        payload = decode_token(token)
        user = db.query(User).filter(User.id == int(payload["sub"])).first()
        if not user:
            raise HTTPException(status_code=401, detail="User not found")
        return user
    except HTTPException:
        raise
    except Exception:
        raise HTTPException(status_code=401, detail="Invalid token")

def require_admin(user: User = Depends(get_current_user)) -> User:
    if not user.is_admin:
        raise HTTPException(status_code=403, detail="Admin only")
    return user

# --- Auth ---

class LoginRequest(BaseModel):
    username: str
    password: str

@app.post("/api/login")
def login(req: LoginRequest, db: Session = Depends(get_db)):
    user = db.query(User).filter(User.username == req.username).first()
    if not user or not verify_password(req.password, user.password_hash):
        raise HTTPException(status_code=401, detail="Invalid credentials")
    return {
        "token": create_token(user.id, user.is_admin),
        "username": user.username,
        "is_admin": user.is_admin,
        "credits": user.credits,
    }

# --- Placeholder routes (filled in later tasks) ---

@app.post("/api/generate")
async def generate_placeholder():
    raise HTTPException(status_code=501, detail="Not implemented")

@app.get("/api/history")
def history_placeholder(user: User = Depends(get_current_user)):
    return []

@app.get("/api/gallery")
def gallery_placeholder(user: User = Depends(get_current_user)):
    return {"images": []}

@app.post("/api/admin/users")
def admin_users_placeholder(admin: User = Depends(require_admin)):
    raise HTTPException(status_code=501, detail="Not implemented")

@app.put("/api/admin/users/{user_id}/credits")
def admin_credits_placeholder(user_id: int, admin: User = Depends(require_admin)):
    raise HTTPException(status_code=501, detail="Not implemented")

@app.get("/api/admin/users")
def admin_list_placeholder(admin: User = Depends(require_admin)):
    return []
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest tests/test_auth.py -v
```

Expected: 4 PASSED

- [ ] **Step 5: Commit**

```bash
git add image-gen-api/main.py image-gen-api/tests/test_auth.py
git commit -m "feat(image-gen): FastAPI app + login endpoint"
```

---

## Task 4: Runware client

**Files:**
- Create: `image-gen-api/runware.py`

> Note: Verify the exact Runware REST API request format and model ID from https://docs.runware.ai before deploying. The payload structure below follows Runware's v1 API pattern.

- [ ] **Step 1: Create `image-gen-api/runware.py`**

```python
import os
import uuid
import httpx

RUNWARE_API_URL = "https://api.runware.ai/v1"
CREDITS_PER_IMAGE = 1

async def generate_images(
    prompt: str,
    negative_prompt: str,
    width: int,
    height: int,
    count: int,
    cfg_scale: float,
    steps: int,
) -> list[str]:
    api_key = os.getenv("RUNWARE_API_KEY")
    if not api_key:
        raise RuntimeError("RUNWARE_API_KEY not set")

    payload = [
        {
            "taskType": "imageInference",
            "taskUUID": str(uuid.uuid4()),
            "positivePrompt": prompt,
            "negativePrompt": negative_prompt,
            "model": "klingai:klingai-image-3-0",
            "width": width,
            "height": height,
            "numberResults": count,
            "CFGScale": cfg_scale,
            "steps": steps,
            "outputFormat": "WEBP",
        }
    ]

    async with httpx.AsyncClient(timeout=90) as client:
        resp = await client.post(
            RUNWARE_API_URL,
            json=payload,
            headers={"Authorization": f"Bearer {api_key}"},
        )
        resp.raise_for_status()
        data = resp.json()
        return [item["imageURL"] for item in data["data"]]
```

- [ ] **Step 2: Verify import works**

```bash
cd image-gen-api
python -c "from runware import generate_images, CREDITS_PER_IMAGE; print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add image-gen-api/runware.py
git commit -m "feat(image-gen): Runware API client"
```

---

## Task 5: Generate endpoint

**Files:**
- Modify: `image-gen-api/main.py` (replace generate_placeholder)
- Create: `image-gen-api/tests/test_generate.py`

- [ ] **Step 1: Write failing generate tests**

Create `image-gen-api/tests/test_generate.py`:

```python
from unittest.mock import patch, AsyncMock

MOCK_URLS = ["https://cdn.runware.ai/img1.webp"]

def _token(client, username, password):
    resp = client.post("/api/login", json={"username": username, "password": password})
    return resp.json()["token"]

def test_generate_success(client):
    token = _token(client, "client1", "pass123")
    with patch("main.generate_images", new_callable=AsyncMock, return_value=MOCK_URLS):
        resp = client.post("/api/generate",
            json={"prompt": "a cat", "count": 1},
            headers={"Authorization": f"Bearer {token}"},
        )
    assert resp.status_code == 200
    assert resp.json()["image_urls"] == MOCK_URLS
    assert resp.json()["credits"] == 9  # started with 10, used 1

def test_generate_deducts_credits_for_multiple(client):
    token = _token(client, "client1", "pass123")
    with patch("main.generate_images", new_callable=AsyncMock, return_value=MOCK_URLS * 2):
        resp = client.post("/api/generate",
            json={"prompt": "a cat", "count": 2},
            headers={"Authorization": f"Bearer {token}"},
        )
    assert resp.status_code == 200
    assert resp.json()["credits"] == 8  # 10 - 2

def test_generate_no_credits(client):
    admin_token = _token(client, "admin", "admin123")
    users = client.get("/api/admin/users", headers={"Authorization": f"Bearer {admin_token}"}).json()
    user_id = next(u["id"] for u in users if u["username"] == "client1")
    client.put(f"/api/admin/users/{user_id}/credits",
        json={"credits": 0},
        headers={"Authorization": f"Bearer {admin_token}"},
    )

    token = _token(client, "client1", "pass123")
    resp = client.post("/api/generate",
        json={"prompt": "a cat", "count": 1},
        headers={"Authorization": f"Bearer {token}"},
    )
    assert resp.status_code == 402

def test_generate_admin_bypasses_credits(client):
    token = _token(client, "admin", "admin123")
    with patch("main.generate_images", new_callable=AsyncMock, return_value=MOCK_URLS):
        resp = client.post("/api/generate",
            json={"prompt": "a cat", "count": 1},
            headers={"Authorization": f"Bearer {token}"},
        )
    assert resp.status_code == 200

def test_generate_no_auth(client):
    resp = client.post("/api/generate", json={"prompt": "a cat"})
    assert resp.status_code in (401, 422)
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/test_generate.py -v
```

Expected: most tests FAIL or ERROR (placeholder returns 501)

- [ ] **Step 3: Replace generate_placeholder in `main.py`**

Add imports at the top of main.py (after existing imports):
```python
from runware import generate_images, CREDITS_PER_IMAGE
```

Replace the `generate_placeholder` function with:

```python
class GenerateRequest(BaseModel):
    prompt: str
    negative_prompt: str = ""
    width: int = 1024
    height: int = 1024
    count: int = 1
    cfg_scale: float = 7.0
    steps: int = 28

@app.post("/api/generate")
async def generate(req: GenerateRequest, user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    if not user.is_admin:
        cost = req.count * CREDITS_PER_IMAGE
        if user.credits < cost:
            raise HTTPException(status_code=402, detail="Insufficient credits")

    image_urls = await generate_images(
        prompt=req.prompt,
        negative_prompt=req.negative_prompt,
        width=req.width,
        height=req.height,
        count=req.count,
        cfg_scale=req.cfg_scale,
        steps=req.steps,
    )

    if not user.is_admin:
        user.credits -= req.count * CREDITS_PER_IMAGE

    gen = Generation(
        user_id=user.id,
        prompt=req.prompt,
        negative_prompt=req.negative_prompt,
        settings={"width": req.width, "height": req.height, "cfg_scale": req.cfg_scale, "steps": req.steps},
        image_urls=image_urls,
    )
    db.add(gen)
    db.commit()
    db.refresh(gen)

    return {"id": gen.id, "image_urls": image_urls, "credits": user.credits}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest tests/test_generate.py -v
```

Expected: 5 PASSED

- [ ] **Step 5: Commit**

```bash
git add image-gen-api/main.py image-gen-api/tests/test_generate.py
git commit -m "feat(image-gen): generate endpoint with credits check"
```

---

## Task 6: History + Gallery endpoints

**Files:**
- Modify: `image-gen-api/main.py` (replace placeholders)
- Create: `image-gen-api/tests/test_history.py`

- [ ] **Step 1: Write failing tests**

Create `image-gen-api/tests/test_history.py`:

```python
from unittest.mock import patch, AsyncMock

MOCK_URLS = ["https://cdn.runware.ai/img1.webp"]

def _token(client, username="client1", password="pass123"):
    return client.post("/api/login", json={"username": username, "password": password}).json()["token"]

def _generate(client, token, prompt="a cat"):
    with patch("main.generate_images", new_callable=AsyncMock, return_value=MOCK_URLS):
        client.post("/api/generate",
            json={"prompt": prompt, "count": 1},
            headers={"Authorization": f"Bearer {token}"},
        )

def test_history_empty(client):
    token = _token(client)
    resp = client.get("/api/history", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 200
    assert resp.json() == []

def test_history_after_generate(client):
    token = _token(client)
    _generate(client, token, "a dog")
    resp = client.get("/api/history", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 200
    assert len(resp.json()) == 1
    assert resp.json()[0]["prompt"] == "a dog"
    assert resp.json()[0]["image_urls"] == MOCK_URLS

def test_gallery_returns_flat_urls(client):
    token = _token(client)
    _generate(client, token)
    _generate(client, token)
    resp = client.get("/api/gallery", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 200
    assert len(resp.json()["images"]) == 2

def test_history_isolated_per_user(client):
    # client1 generates something
    token1 = _token(client, "client1", "pass123")
    _generate(client, token1)

    # admin should have empty history
    token2 = _token(client, "admin", "admin123")
    resp = client.get("/api/history", headers={"Authorization": f"Bearer {token2}"})
    assert resp.json() == []
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/test_history.py -v
```

Expected: test_history_empty PASSES (placeholder returns []), others FAIL

- [ ] **Step 3: Replace history and gallery placeholders in `main.py`**

Replace `history_placeholder`:

```python
@app.get("/api/history")
def get_history(user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    gens = (
        db.query(Generation)
        .filter(Generation.user_id == user.id)
        .order_by(Generation.created_at.desc())
        .limit(50)
        .all()
    )
    return [
        {
            "id": g.id,
            "prompt": g.prompt,
            "image_urls": g.image_urls,
            "settings": g.settings,
            "created_at": g.created_at.isoformat(),
        }
        for g in gens
    ]
```

Replace `gallery_placeholder`:

```python
@app.get("/api/gallery")
def get_gallery(user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    gens = (
        db.query(Generation)
        .filter(Generation.user_id == user.id)
        .order_by(Generation.created_at.desc())
        .all()
    )
    return {"images": [url for g in gens for url in g.image_urls]}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest tests/test_history.py -v
```

Expected: 4 PASSED

- [ ] **Step 5: Commit**

```bash
git add image-gen-api/main.py image-gen-api/tests/test_history.py
git commit -m "feat(image-gen): history and gallery endpoints"
```

---

## Task 7: Admin endpoints

**Files:**
- Modify: `image-gen-api/main.py` (replace admin placeholders)
- Create: `image-gen-api/tests/test_admin.py`

- [ ] **Step 1: Write failing admin tests**

Create `image-gen-api/tests/test_admin.py`:

```python
def _token(client, username, password):
    return client.post("/api/login", json={"username": username, "password": password}).json()["token"]

def test_list_users(client):
    token = _token(client, "admin", "admin123")
    resp = client.get("/api/admin/users", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 200
    users = resp.json()
    assert len(users) == 1
    assert users[0]["username"] == "client1"
    assert users[0]["credits"] == 10

def test_create_user(client):
    token = _token(client, "admin", "admin123")
    resp = client.post("/api/admin/users",
        json={"username": "newclient", "password": "pass", "credits": 50},
        headers={"Authorization": f"Bearer {token}"},
    )
    assert resp.status_code == 200
    assert resp.json()["username"] == "newclient"
    assert resp.json()["credits"] == 50

def test_create_user_duplicate_fails(client):
    token = _token(client, "admin", "admin123")
    resp = client.post("/api/admin/users",
        json={"username": "client1", "password": "x"},
        headers={"Authorization": f"Bearer {token}"},
    )
    assert resp.status_code == 400

def test_create_user_non_admin_forbidden(client):
    token = _token(client, "client1", "pass123")
    resp = client.post("/api/admin/users",
        json={"username": "x", "password": "y"},
        headers={"Authorization": f"Bearer {token}"},
    )
    assert resp.status_code == 403

def test_update_credits(client):
    token = _token(client, "admin", "admin123")
    users = client.get("/api/admin/users", headers={"Authorization": f"Bearer {token}"}).json()
    user_id = users[0]["id"]

    resp = client.put(f"/api/admin/users/{user_id}/credits",
        json={"credits": 200},
        headers={"Authorization": f"Bearer {token}"},
    )
    assert resp.status_code == 200
    assert resp.json()["credits"] == 200

def test_update_credits_unknown_user(client):
    token = _token(client, "admin", "admin123")
    resp = client.put("/api/admin/users/9999/credits",
        json={"credits": 10},
        headers={"Authorization": f"Bearer {token}"},
    )
    assert resp.status_code == 404
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/test_admin.py -v
```

Expected: test_list_users and test_create_user_non_admin_forbidden may pass; others FAIL (501)

- [ ] **Step 3: Replace admin placeholders in `main.py`**

Replace `admin_users_placeholder`:

```python
class CreateUserRequest(BaseModel):
    username: str
    password: str
    credits: int = 0

@app.post("/api/admin/users")
def create_user(req: CreateUserRequest, admin: User = Depends(require_admin), db: Session = Depends(get_db)):
    if db.query(User).filter(User.username == req.username).first():
        raise HTTPException(status_code=400, detail="Username already exists")
    user = User(
        username=req.username,
        password_hash=hash_password(req.password),
        credits=req.credits,
        is_admin=False,
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return {"id": user.id, "username": user.username, "credits": user.credits}
```

Replace `admin_credits_placeholder`:

```python
class UpdateCreditsRequest(BaseModel):
    credits: int

@app.put("/api/admin/users/{user_id}/credits")
def update_credits(user_id: int, req: UpdateCreditsRequest, admin: User = Depends(require_admin), db: Session = Depends(get_db)):
    user = db.query(User).filter(User.id == user_id).first()
    if not user:
        raise HTTPException(status_code=404, detail="User not found")
    user.credits = req.credits
    db.commit()
    return {"id": user.id, "username": user.username, "credits": user.credits}
```

Replace `admin_list_placeholder`:

```python
@app.get("/api/admin/users")
def list_users(admin: User = Depends(require_admin), db: Session = Depends(get_db)):
    users = db.query(User).filter(User.is_admin == False).all()
    return [
        {"id": u.id, "username": u.username, "credits": u.credits, "created_at": u.created_at.isoformat()}
        for u in users
    ]
```

- [ ] **Step 4: Run all tests**

```bash
pytest tests/ -v
```

Expected: all PASSED

- [ ] **Step 5: Commit**

```bash
git add image-gen-api/main.py image-gen-api/tests/test_admin.py
git commit -m "feat(image-gen): admin endpoints — create user, manage credits"
```

---

## Task 8: Frontend — login page

**Files:**
- Create: `image-gen/login.html`

- [ ] **Step 1: Create `image-gen/login.html`**

```html
<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Вход — AI Генератор</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'Segoe UI', system-ui, sans-serif; background: #0f0f13; color: #e8e8f0; min-height: 100vh; display: flex; align-items: center; justify-content: center; }
  .card { background: #15151f; border: 1px solid #2a2a40; border-radius: 16px; padding: 40px; width: 360px; }
  h1 { font-size: 22px; font-weight: 700; margin-bottom: 8px; }
  .sub { font-size: 14px; color: #666; margin-bottom: 32px; }
  label { display: block; font-size: 12px; font-weight: 600; color: #888; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 6px; }
  input { width: 100%; background: #0f0f13; border: 1px solid #2a2a40; border-radius: 8px; color: #e8e8f0; font-size: 14px; padding: 10px 14px; outline: none; margin-bottom: 20px; font-family: inherit; }
  input:focus { border-color: #7c6af7; }
  button { width: 100%; background: linear-gradient(135deg, #7c6af7, #a855f7); color: #fff; border: none; border-radius: 10px; padding: 13px; font-size: 15px; font-weight: 700; cursor: pointer; }
  .error { color: #f87171; font-size: 13px; margin-top: 14px; text-align: center; display: none; }
</style>
</head>
<body>
<div class="card">
  <h1>AI Генератор</h1>
  <p class="sub">Войдите чтобы начать генерировать</p>
  <label>Логин</label>
  <input type="text" id="username" placeholder="Ваш логин">
  <label>Пароль</label>
  <input type="password" id="password" placeholder="Пароль">
  <button onclick="login()">Войти</button>
  <div class="error" id="err">Неверный логин или пароль</div>
</div>
<script>
  const API = 'REPLACE_WITH_RAILWAY_URL';

  if (localStorage.getItem('token')) location.href = 'index.html';

  async function login() {
    const username = document.getElementById('username').value.trim();
    const password = document.getElementById('password').value;
    const resp = await fetch(`${API}/api/login`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username, password }),
    });
    if (!resp.ok) {
      document.getElementById('err').style.display = 'block';
      return;
    }
    const data = await resp.json();
    localStorage.setItem('token', data.token);
    localStorage.setItem('username', data.username);
    localStorage.setItem('is_admin', data.is_admin);
    localStorage.setItem('credits', data.credits);
    location.href = data.is_admin ? 'admin.html' : 'index.html';
  }

  document.addEventListener('keydown', e => { if (e.key === 'Enter') login(); });
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add image-gen/login.html
git commit -m "feat(image-gen): login page"
```

---

## Task 9: Frontend — generator page

**Files:**
- Create: `image-gen/index.html`

- [ ] **Step 1: Create `image-gen/index.html`**

```html
<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI Генератор изображений</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'Segoe UI', system-ui, sans-serif; background: #0f0f13; color: #e8e8f0; min-height: 100vh; }
  .header { display: flex; align-items: center; justify-content: space-between; padding: 16px 28px; border-bottom: 1px solid #1e1e2e; }
  .logo { font-size: 17px; font-weight: 700; }
  .logo span { color: #7c6af7; }
  .user-info { display: flex; align-items: center; gap: 12px; font-size: 13px; color: #888; }
  .credits-badge { background: #1a1a2e; border: 1px solid #2a2a40; border-radius: 20px; padding: 5px 12px; color: #e8e8f0; }
  .credits-badge span { color: #7c6af7; font-weight: 700; }
  .btn-logout { background: none; border: 1px solid #2a2a40; border-radius: 6px; padding: 5px 12px; color: #666; cursor: pointer; font-size: 12px; }
  .layout { display: grid; grid-template-columns: 300px 1fr; height: calc(100vh - 57px); }
  .panel-left { border-right: 1px solid #1e1e2e; padding: 20px; overflow-y: auto; display: flex; flex-direction: column; gap: 16px; }
  .field-group { display: flex; flex-direction: column; gap: 6px; }
  label { font-size: 11px; font-weight: 600; color: #888; text-transform: uppercase; letter-spacing: 0.5px; }
  textarea, select { background: #15151f; border: 1px solid #2a2a40; border-radius: 8px; color: #e8e8f0; font-size: 13px; padding: 10px 12px; outline: none; font-family: inherit; width: 100%; }
  textarea { resize: none; line-height: 1.5; }
  textarea:focus, select:focus { border-color: #7c6af7; }
  .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .range-row { display: flex; align-items: center; gap: 8px; }
  input[type="range"] { flex: 1; accent-color: #7c6af7; }
  .rval { font-size: 13px; color: #7c6af7; font-weight: 700; min-width: 28px; text-align: right; }
  .btn-gen { background: linear-gradient(135deg, #7c6af7, #a855f7); color: #fff; border: none; border-radius: 10px; padding: 13px; font-size: 14px; font-weight: 700; cursor: pointer; width: 100%; }
  .btn-gen:disabled { opacity: 0.4; cursor: not-allowed; }
  .panel-right { display: flex; flex-direction: column; background: #0b0b10; }
  .tabs { display: flex; border-bottom: 1px solid #1e1e2e; padding: 0 20px; }
  .tab { padding: 13px 18px; font-size: 13px; color: #555; cursor: pointer; border-bottom: 2px solid transparent; margin-bottom: -1px; }
  .tab.active { color: #e8e8f0; border-color: #7c6af7; }
  .pane { display: none; flex: 1; overflow-y: auto; padding: 20px; }
  .pane.active { display: block; }
  .spinner-wrap { display: flex; align-items: center; gap: 10px; background: #15151f; border: 1px solid #2a2a40; border-radius: 10px; padding: 16px; margin-bottom: 16px; }
  .spinner { width: 18px; height: 18px; border: 2px solid #2a2a40; border-top-color: #7c6af7; border-radius: 50%; animation: spin 0.7s linear infinite; }
  @keyframes spin { to { transform: rotate(360deg); } }
  .img-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 10px; }
  .img-wrap { border-radius: 10px; overflow: hidden; background: #15151f; border: 1px solid #1e1e2e; }
  .img-wrap img { width: 100%; display: block; }
  .img-footer { padding: 8px 10px; display: flex; gap: 6px; }
  .btn-dl, .btn-reuse { flex: 1; background: #1e1e2e; border: none; border-radius: 6px; padding: 6px; font-size: 11px; color: #aaa; cursor: pointer; }
  .btn-dl:hover, .btn-reuse:hover { background: #2a2a40; color: #fff; }
  .hist-item { display: flex; gap: 12px; align-items: center; padding: 12px; background: #15151f; border: 1px solid #1e1e2e; border-radius: 10px; margin-bottom: 8px; cursor: pointer; }
  .hist-thumb { width: 48px; height: 48px; border-radius: 6px; object-fit: cover; background: #1a1a2e; flex-shrink: 0; }
  .hist-text { flex: 1; min-width: 0; }
  .hist-prompt { font-size: 13px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .hist-date { font-size: 11px; color: #555; margin-top: 3px; }
  .empty { color: #555; font-size: 14px; text-align: center; padding: 40px; }
</style>
</head>
<body>
<div class="header">
  <div class="logo">pixel<span>AI</span></div>
  <div class="user-info">
    <div class="credits-badge">Кредитов: <span id="credits-count">—</span></div>
    <button class="btn-logout" onclick="logout()">Выйти</button>
  </div>
</div>
<div class="layout">
  <div class="panel-left">
    <div class="field-group">
      <label>Промпт</label>
      <textarea id="prompt" rows="5" placeholder="Опиши что хочешь..."></textarea>
    </div>
    <div class="field-group">
      <label>Негативный промпт</label>
      <textarea id="neg" rows="3" placeholder="Что исключить...">blurry, low quality, deformed</textarea>
    </div>
    <div class="grid-2">
      <div class="field-group">
        <label>Размер</label>
        <select id="size">
          <option value="1024,1024">1024×1024</option>
          <option value="1920,1080">1920×1080</option>
          <option value="1080,1920">1080×1920</option>
          <option value="768,1344">768×1344</option>
        </select>
      </div>
      <div class="field-group">
        <label>Кол-во</label>
        <select id="count">
          <option value="1">1</option>
          <option value="2">2</option>
          <option value="4">4</option>
        </select>
      </div>
    </div>
    <div class="field-group">
      <label>CFG Scale</label>
      <div class="range-row">
        <input type="range" id="cfg" min="1" max="20" value="7" oninput="document.getElementById('cfg-v').textContent=this.value">
        <div class="rval" id="cfg-v">7</div>
      </div>
    </div>
    <div class="field-group">
      <label>Steps</label>
      <div class="range-row">
        <input type="range" id="steps" min="10" max="50" value="28" oninput="document.getElementById('steps-v').textContent=this.value">
        <div class="rval" id="steps-v">28</div>
      </div>
    </div>
    <button class="btn-gen" id="gen-btn" onclick="generate()">&#10022; Сгенерировать</button>
  </div>
  <div class="panel-right">
    <div class="tabs">
      <div class="tab active" onclick="switchTab('result')">Результат</div>
      <div class="tab" onclick="switchTab('history')">История</div>
      <div class="tab" onclick="switchTab('gallery')">Галерея</div>
    </div>
    <div class="pane active" id="pane-result">
      <div class="img-grid" id="result-grid"></div>
    </div>
    <div class="pane" id="pane-history">
      <div id="history-list"><div class="empty">История пуста</div></div>
    </div>
    <div class="pane" id="pane-gallery">
      <div class="img-grid" id="gallery-grid"></div>
    </div>
  </div>
</div>
<script>
  const API = 'REPLACE_WITH_RAILWAY_URL';
  const token = localStorage.getItem('token');
  if (!token) location.href = 'login.html';

  document.getElementById('credits-count').textContent = localStorage.getItem('credits') || '0';

  function logout() {
    localStorage.clear();
    location.href = 'login.html';
  }

  function switchTab(name) {
    document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('active', ['result','history','gallery'][i] === name));
    document.querySelectorAll('.pane').forEach(p => p.classList.remove('active'));
    document.getElementById(`pane-${name}`).classList.add('active');
    if (name === 'history') loadHistory();
    if (name === 'gallery') loadGallery();
  }

  function imgCard(url, prompt) {
    return `<div class="img-wrap">
      <img src="${url}" alt="${prompt}" loading="lazy">
      <div class="img-footer">
        <button class="btn-dl" onclick="download('${url}')">&#8595; Скачать</button>
        <button class="btn-reuse" onclick="reusePrompt('${prompt.replace(/'/g,"\\'")}')">&#8635; Повтор</button>
      </div>
    </div>`;
  }

  async function generate() {
    const prompt = document.getElementById('prompt').value.trim();
    if (!prompt) return;
    const [width, height] = document.getElementById('size').value.split(',').map(Number);
    const count = Number(document.getElementById('count').value);

    const btn = document.getElementById('gen-btn');
    btn.disabled = true;
    btn.textContent = 'Генерирую...';

    const grid = document.getElementById('result-grid');
    grid.innerHTML = '<div class="spinner-wrap"><div class="spinner"></div><span style="color:#888;font-size:13px">Генерирую изображения...</span></div>';
    switchTab('result');

    const resp = await fetch(`${API}/api/generate`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` },
      body: JSON.stringify({
        prompt,
        negative_prompt: document.getElementById('neg').value,
        width, height, count,
        cfg_scale: Number(document.getElementById('cfg').value),
        steps: Number(document.getElementById('steps').value),
      }),
    });

    btn.disabled = false;
    btn.textContent = '✦ Сгенерировать';

    if (!resp.ok) {
      const err = await resp.json();
      grid.innerHTML = `<div class="empty" style="color:#f87171">${err.detail || 'Ошибка генерации'}</div>`;
      return;
    }

    const data = await resp.json();
    localStorage.setItem('credits', data.credits);
    document.getElementById('credits-count').textContent = data.credits;
    grid.innerHTML = data.image_urls.map(url => imgCard(url, prompt)).join('');
  }

  async function loadHistory() {
    const resp = await fetch(`${API}/api/history`, { headers: { 'Authorization': `Bearer ${token}` } });
    const items = await resp.json();
    const el = document.getElementById('history-list');
    if (!items.length) { el.innerHTML = '<div class="empty">История пуста</div>'; return; }
    el.innerHTML = items.map(g => `
      <div class="hist-item" onclick="reusePrompt('${g.prompt.replace(/'/g,"\\'")}')">
        <img class="hist-thumb" src="${g.image_urls[0] || ''}" alt="">
        <div class="hist-text">
          <div class="hist-prompt">${g.prompt}</div>
          <div class="hist-date">${new Date(g.created_at).toLocaleString('ru')}</div>
        </div>
      </div>`).join('');
  }

  async function loadGallery() {
    const resp = await fetch(`${API}/api/gallery`, { headers: { 'Authorization': `Bearer ${token}` } });
    const data = await resp.json();
    const grid = document.getElementById('gallery-grid');
    if (!data.images.length) { grid.innerHTML = '<div class="empty">Галерея пуста</div>'; return; }
    grid.innerHTML = data.images.map(url => `
      <div class="img-wrap">
        <img src="${url}" loading="lazy">
        <div class="img-footer"><button class="btn-dl" onclick="download('${url}')">&#8595; Скачать</button></div>
      </div>`).join('');
  }

  function reusePrompt(prompt) {
    document.getElementById('prompt').value = prompt;
    switchTab('result');
  }

  function download(url) {
    const a = document.createElement('a');
    a.href = url;
    a.download = 'image.webp';
    a.click();
  }
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add image-gen/index.html
git commit -m "feat(image-gen): generator page"
```

---

## Task 10: Frontend — admin page

**Files:**
- Create: `image-gen/admin.html`

- [ ] **Step 1: Create `image-gen/admin.html`**

```html
<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Админ — AI Генератор</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'Segoe UI', system-ui, sans-serif; background: #0f0f13; color: #e8e8f0; min-height: 100vh; }
  .header { display: flex; align-items: center; justify-content: space-between; padding: 16px 28px; border-bottom: 1px solid #1e1e2e; }
  .logo { font-size: 17px; font-weight: 700; }
  .logo span { color: #7c6af7; }
  .links { display: flex; gap: 10px; }
  .btn-link { background: none; border: 1px solid #2a2a40; border-radius: 6px; padding: 5px 12px; color: #888; cursor: pointer; font-size: 12px; text-decoration: none; display: inline-block; }
  .main { max-width: 900px; margin: 32px auto; padding: 0 20px; }
  h2 { font-size: 16px; font-weight: 700; margin-bottom: 16px; color: #aaa; }
  .card { background: #15151f; border: 1px solid #2a2a40; border-radius: 12px; padding: 24px; margin-bottom: 28px; }
  .grid-form { display: grid; grid-template-columns: 1fr 1fr 120px 100px; gap: 10px; align-items: end; }
  label { display: block; font-size: 11px; font-weight: 600; color: #888; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 5px; }
  input { width: 100%; background: #0f0f13; border: 1px solid #2a2a40; border-radius: 7px; color: #e8e8f0; font-size: 13px; padding: 9px 12px; outline: none; font-family: inherit; }
  input:focus { border-color: #7c6af7; }
  .btn { background: linear-gradient(135deg, #7c6af7, #a855f7); color: #fff; border: none; border-radius: 8px; padding: 9px 16px; font-size: 13px; font-weight: 700; cursor: pointer; width: 100%; }
  table { width: 100%; border-collapse: collapse; font-size: 13px; }
  th { text-align: left; font-size: 11px; font-weight: 600; color: #666; text-transform: uppercase; letter-spacing: 0.5px; padding: 8px 10px; border-bottom: 1px solid #1e1e2e; }
  td { padding: 10px; border-bottom: 1px solid #1a1a2e; vertical-align: middle; }
  tr:last-child td { border-bottom: none; }
  .credits-input { width: 80px; background: #0f0f13; border: 1px solid #2a2a40; border-radius: 6px; color: #e8e8f0; font-size: 13px; padding: 6px 8px; outline: none; font-family: inherit; }
  .btn-sm { background: #7c6af7; color: #fff; border: none; border-radius: 6px; padding: 6px 12px; font-size: 12px; cursor: pointer; }
  .msg { font-size: 13px; color: #22c55e; margin-top: 10px; display: none; }
</style>
</head>
<body>
<div class="header">
  <div class="logo">pixel<span>AI</span> — Админ</div>
  <div class="links">
    <a class="btn-link" href="index.html">Генератор</a>
    <button class="btn-link" onclick="logout()">Выйти</button>
  </div>
</div>
<div class="main">
  <div class="card">
    <h2>Создать клиента</h2>
    <div class="grid-form">
      <div class="field-group">
        <label>Логин</label>
        <input type="text" id="new-username" placeholder="username">
      </div>
      <div class="field-group">
        <label>Пароль</label>
        <input type="password" id="new-password" placeholder="пароль">
      </div>
      <div class="field-group">
        <label>Кредиты</label>
        <input type="number" id="new-credits" value="50" min="0">
      </div>
      <div class="field-group">
        <label>&nbsp;</label>
        <button class="btn" onclick="createUser()">Создать</button>
      </div>
    </div>
    <div class="msg" id="create-msg">Клиент создан</div>
  </div>

  <div class="card">
    <h2>Клиенты</h2>
    <table>
      <thead>
        <tr>
          <th>Логин</th>
          <th>Кредиты</th>
          <th>Дата регистрации</th>
          <th>Изменить кредиты</th>
        </tr>
      </thead>
      <tbody id="users-table"></tbody>
    </table>
  </div>
</div>
<script>
  const API = 'REPLACE_WITH_RAILWAY_URL';
  const token = localStorage.getItem('token');
  const isAdmin = localStorage.getItem('is_admin') === 'true';
  if (!token || !isAdmin) location.href = 'login.html';

  async function loadUsers() {
    const resp = await fetch(`${API}/api/admin/users`, { headers: { 'Authorization': `Bearer ${token}` } });
    const users = await resp.json();
    document.getElementById('users-table').innerHTML = users.map(u => `
      <tr>
        <td>${u.username}</td>
        <td id="cr-${u.id}">${u.credits}</td>
        <td>${new Date(u.created_at).toLocaleDateString('ru')}</td>
        <td style="display:flex;gap:8px;align-items:center">
          <input class="credits-input" type="number" id="inp-${u.id}" value="${u.credits}" min="0">
          <button class="btn-sm" onclick="setCredits(${u.id})">Сохранить</button>
        </td>
      </tr>`).join('');
  }

  async function createUser() {
    const username = document.getElementById('new-username').value.trim();
    const password = document.getElementById('new-password').value;
    const credits = Number(document.getElementById('new-credits').value);
    if (!username || !password) return;

    const resp = await fetch(`${API}/api/admin/users`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` },
      body: JSON.stringify({ username, password, credits }),
    });
    if (resp.ok) {
      document.getElementById('new-username').value = '';
      document.getElementById('new-password').value = '';
      const msg = document.getElementById('create-msg');
      msg.style.display = 'block';
      setTimeout(() => msg.style.display = 'none', 3000);
      loadUsers();
    } else {
      const err = await resp.json();
      alert(err.detail);
    }
  }

  async function setCredits(userId) {
    const credits = Number(document.getElementById(`inp-${userId}`).value);
    const resp = await fetch(`${API}/api/admin/users/${userId}/credits`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` },
      body: JSON.stringify({ credits }),
    });
    if (resp.ok) {
      document.getElementById(`cr-${userId}`).textContent = credits;
    }
  }

  function logout() { localStorage.clear(); location.href = 'login.html'; }

  loadUsers();
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add image-gen/admin.html
git commit -m "feat(image-gen): admin page"
```

---

## Task 11: Deploy to Railway

- [ ] **Step 1: Create new Railway service**

В Railway → New Project → Deploy from GitHub repo (или Empty Service → upload folder).  
Если деплоишь папку — скопируй `image-gen-api/` как отдельный репо или укажи Root Directory в настройках Railway.

- [ ] **Step 2: Add PostgreSQL plugin**

В Railway → Add Plugin → PostgreSQL. Railway сам пропишет `DATABASE_URL` в env.

- [ ] **Step 3: Set environment variables in Railway**

```
RUNWARE_API_KEY=your-key-here
JWT_SECRET=generate-random-string-here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password
SEED_ADMIN=true
```

Для генерации JWT_SECRET: `python -c "import secrets; print(secrets.token_hex(32))"`

- [ ] **Step 4: Get Railway URL after deploy**

После деплоя Railway даст URL вида `https://your-service.up.railway.app`.

- [ ] **Step 5: Replace `REPLACE_WITH_RAILWAY_URL` в всех трёх HTML файлах**

```bash
# Заменить в login.html, index.html, admin.html:
sed -i "s|REPLACE_WITH_RAILWAY_URL|https://your-service.up.railway.app|g" image-gen/login.html image-gen/index.html image-gen/admin.html
```

- [ ] **Step 6: Commit и деплой фронта**

```bash
git add image-gen/
git commit -m "feat(image-gen): set Railway API URL"
git push
```

Netlify задеплоит автоматически.

- [ ] **Step 7: Проверить что всё работает**

1. Открыть `https://твой-сайт/image-gen/login.html`
2. Войти с ADMIN_USERNAME / ADMIN_PASSWORD
3. Создать тестового клиента в admin.html
4. Войти как клиент → сгенерировать картинку
5. Проверить что кредиты списались

---

## Spec Coverage Check

| Требование из спека | Задача |
|---------------------|--------|
| 3 HTML страницы (генератор, логин, админ) | Task 8, 9, 10 |
| FastAPI бэкенд | Task 3 |
| PostgreSQL (users + generations) | Task 1, 2 |
| POST /api/login → JWT | Task 3 |
| POST /api/generate → Runware → кредиты | Task 5 |
| GET /api/history | Task 6 |
| GET /api/gallery | Task 6 |
| POST /api/admin/users | Task 7 |
| PUT /api/admin/users/{id}/credits | Task 7 |
| GET /api/admin/users | Task 7 |
| Владелец без лимита кредитов | Task 5 |
| Деплой Railway + Netlify | Task 11 |
